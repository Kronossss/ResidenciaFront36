import { Routes } from '@angular/router';
import { AppComponent } from './app.component';  // Importando o componente principal

export const routes: Routes = [
  { path: '', component: AppComponent },  // Definindo a rota raiz para carregar o AppComponent
];